name: my first workflow

on: push

jobs:
    first_jobs:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "my first github actions"
          
        - name: list files
          run: ls
        - name: read file
          run: cat README.md  